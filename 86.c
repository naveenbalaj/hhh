#include<stdio.h>
#include<string.h>
int main() 
{
char str[50];
printf("enter the string:");
scanf("%s",str);
if(strrev(str[50])=str)	
printf("isomorphism");
else
printf("not isomorphism");
return 0;
}
